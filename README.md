
  # Build Hero Section

  This is a code bundle for Build Hero Section. The original project is available at https://www.figma.com/design/lRrtT8R2wsRlrP0Mt2Jtao/Build-Hero-Section.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  